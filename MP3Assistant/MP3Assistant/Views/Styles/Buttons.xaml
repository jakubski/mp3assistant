<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MP3Assistant"
                    x:Name="ButtonStyles">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
        <ResourceDictionary Source="Fonts.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Base style of all buttons (removing the silly hover effect) -->
    <Style x:Key="BaseButton" TargetType="{x:Type Button}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Navigation buttons -->
    <Style x:Key="BaseNavigationButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource GoldenDarkBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}"/>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource GoldenLightBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource GrayBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Major navigation buttons -->
    <Style x:Key="BaseMajorNavigationButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseNavigationButton}">
        <Setter Property="FontSize" Value="36"/>
    </Style>
    
    <Style x:Key="MajorBackButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseMajorNavigationButton}">
        <Setter Property="Content" Value="&#xf0a8;"/>        
    </Style>

    <Style x:Key="MajorNextButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseMajorNavigationButton}">
        <Setter Property="Content" Value="&#xf0a9;"/>
    </Style>
    
    <!-- Medium navigation buttons -->
    <Style x:Key="BaseMediumNavigationButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseNavigationButton}">
        <Setter Property="FontSize" Value="22"/>
    </Style>

    <Style x:Key="MediumBackButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseMediumNavigationButton}">
        <Setter Property="Content" Value="&#xf359;"/>
    </Style>

    <Style x:Key="MediumNextButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseMediumNavigationButton}">
        <Setter Property="Content" Value="&#xf35a;"/>
    </Style>
    
    <!-- Text on rounded background button -->
    <Style x:Key="RoundedButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseButton}">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="ButtonBorder"
                            CornerRadius="16"
                            BorderThickness="0"
                            Background="{Binding Path=Background,
                                                 RelativeSource={RelativeSource Mode=TemplatedParent}}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{Binding Path=Foreground,
                                                                           RelativeSource={RelativeSource Mode=TemplatedParent},
                                                                           FallbackValue={StaticResource LightBrush}}"
                                          TextElement.FontWeight="Bold"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Standard button -->
    <Style x:Key="GoldenRoundedButton" TargetType="{x:Type Button}" BasedOn="{StaticResource RoundedButton}">
        <Setter Property="Foreground" Value="{StaticResource DarkBrush}"/>
        <Setter Property="Background" Value="{StaticResource GoldenDarkBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource GoldenLightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Confirm button -->
    <Style x:Key="GreenRoundedButton" TargetType="{x:Type Button}" BasedOn="{StaticResource RoundedButton}">
        <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
        <Setter Property="Background" Value="{StaticResource GreenDarkBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource GreenLightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Cancel button -->
    <Style x:Key="RedRoundedButton" TargetType="{x:Type Button}" BasedOn="{StaticResource RoundedButton}">
        <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
        <Setter Property="Background" Value="{StaticResource RedDarkBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource RedLightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Text on transparent background button -->
    <Style x:Key="TransparentButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseButton}">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource GoldenDarkBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource GoldenLightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Settings button -->
    <Style x:Key="SettingsButton" TargetType="{x:Type Button}" BasedOn="{StaticResource TransparentButton}">
        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}"/>
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="Content" Value="&#xf013;"/>
    </Style>

</ResourceDictionary>